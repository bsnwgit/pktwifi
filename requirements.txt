# pktWiFi Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# App database
aiosqlite>=0.20.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2
httpx>=0.27.0
python3-saml>=1.16.0
pyasn1<0.6.1,>=0.4.8  # pysnmp-lextudio 6.3.0 hard-requires <0.6.1 and still imports the
                       # pyasn1.compat.octets shim removed in 0.6.4 - bumping past this
                       # ceiling breaks every SNMP call at runtime (lazy-imported, so it
                       # doesn't fail until the first poll, not at process start). The
                       # pyasn1 CVEs this would have closed (PYSEC-2026-2263/3455/3456/3457)
                       # stay open until pysnmp-lextudio ships a release compatible with a
                       # newer pyasn1 - tracked as a known accepted risk, not fixed here.

# Notifications
aiosmtplib>=3.0.1
jinja2>=3.1.4

# Collector credential encryption (Fernet)
cryptography>=42.0.0

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

# SNMP (generic collector)
pysnmp-lextudio>=6.1.0

# Utilities
python-dateutil>=2.9.0
